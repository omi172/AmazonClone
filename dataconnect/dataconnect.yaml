specVersion: "v1"
serviceId: "amazon"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "amazon-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
